#include "PuzzleFactory.h"

/**
 * create a puzzle
 * @param p a source puzzle to base the new one off of
 * @return a Sudoku* with similar but randomized traits
 */
Sudoku* PuzzleFactory::createPuzzle(Sudoku* p) {
    return nullptr;
}